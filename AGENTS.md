# Agent Instructions

- Close applications or games started during a task when the task is finished, unless they still need to remain running.
- For complex project work, read `codex.md` before starting.
- When important project-specific context is learned, add it to `codex.md`.
