{
  "$schema": "Canonical design tokens for Meridian's three frontend surfaces (Insights, Care, Family). This file is the single source of truth. Next.js consumes it via lib/tokens.ts + globals.css CSS vars (hand-mirrored, see file header there). SwiftUI (Care, Family) consumes it via DesignSystem/Tokens.swift in each app (hand-mirrored, same values). If you change a value here, update all three mirrors in the same commit.",
  "color": {
    "primary": "#0369A1",
    "primaryAlt": "#0891B2",
    "success": "#059669",
    "warning": "#B45309",
    "destructive": "#DC2626",
    "foreground": "#0C4A6E",
    "muted": "#E7EFF5",
    "border": "#E0F2FE",
    "background": {
      "dashboard": "#F0F9FF",
      "mobile": "#ECFEFF"
    },
    "onPrimary": "#FFFFFF",
    "onDestructive": "#FFFFFF",
    "foregroundMuted": "#2F5D77",
    "successStrong": "#047857",
    "warningStrong": "#92400E",
    "destructiveStrong": "#991B1B",
    "_assumption_warning": "Base spec defines primary/primary-alt/success/destructive/background/foreground/muted/border but not a 'warning' token, even though IncidentSeverity has an 'info'/'warning'/'critical' three-tier scale distinct from the reserved destructive=emergency red. Added #B45309 (amber-700, 4.5:1+ on both background tones) for the 'warning' severity tier only. destructive (#DC2626) stays reserved exclusively for 'critical'/emergency per the hard constraint. 'info' severity uses foreground/primary tones, not a dedicated color, since it's not alert-worthy.",
    "_accessibility_pass_2026_07_14": "A contrast audit (WCAG relative-luminance formula, computed not eyeballed) found that the initial build's `foreground` at reduced opacity (0.5-0.7, used everywhere for secondary/caption text) failed 4.5:1 on every background, ranging 2.55-4.25:1. It also found severity badge text (color-on-its-own-light-tint) failed for warning/destructive/success (3.14-4.39:1), and white text on the raw `success` button fill failed (3.77:1). Added four solid replacement colors, computed to clear 4.5:1 (most clear 5-8:1, several AAA) against every background they're actually used on: foregroundMuted (secondary/caption text, replaces all opacity-based dimming), successStrong/warningStrong/destructiveStrong (severity badge text on a light tint of the base color, and successStrong doubles as the success button-fill color since raw success fails with white text). The base success/warning/destructive tokens are unchanged and still used for icons, thin accents, and large/bold text where the lower 3:1 threshold applies.",
    "demoSkeleton": {
      "background": "#05060A",
      "accent": "#22FFC2",
      "note": "Intentional one-off palette for the standalone pitch-demo skeleton screen only (spec section 5) — never used elsewhere."
    }
  },
  "contrast": {
    "bodyTextMin": 4.5,
    "target": "AAA where feasible for large text and alert states"
  },
  "typography": {
    "heading": "Figtree",
    "body": "Noto Sans",
    "mono": "Fira Code",
    "monoUsage": "Insights dense/raw-data tables only (timestamps, IDs, raw values) — nowhere else, and never on Care or Family.",
    "iosNote": "Figtree/Noto Sans aren't practical to bundle reliably across two SwiftUI app targets on this timeline; iOS surfaces use San Francisco (system font) at Figtree-equivalent weights (.semibold/.bold for headings, .regular/.medium for body) to match the same 'medical, clean, accessible, trustworthy' mood rather than the literal font file.",
    "baseBodySizePt": 16,
    "scale": {
      "display": 32,
      "title": 24,
      "heading": 20,
      "body": 16,
      "caption": 14,
      "mono": 14
    }
  },
  "motion": {
    "defaultDurationMsMin": 150,
    "defaultDurationMsMax": 300,
    "easing": "ease-in-out",
    "loadingPattern": "skeleton-shimmer",
    "alertArrival": "single one-time scale/pop-in on arrival, then hold steady until acknowledged — never continuous flashing or pulsing",
    "respectReducedMotion": true
  },
  "spacing": {
    "unit": 4,
    "scale": [4, 8, 12, 16, 24, 32, 48, 64]
  },
  "radius": {
    "insights": { "card": 16, "control": 10 },
    "care": { "card": 12, "control": 10 },
    "family": { "card": 20, "control": 14 }
  },
  "touchTarget": {
    "minPt": 44,
    "minSpacingPt": 8,
    "note": "Critical severity issue if violated on Care — a mis-tap on the wrong resident's resolve button during a real emergency is a real failure mode."
  },
  "iconography": {
    "web": "Heroicons or Lucide",
    "ios": "SF Symbols",
    "rule": "No emoji as icons anywhere, including severity/status icons."
  }
}
