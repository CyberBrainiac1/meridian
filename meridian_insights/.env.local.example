# Copy to .env.local and fill in once a live Supabase project exists
# (see /Users/dhairyagurnani/meridian/frontendguytodo.md — nothing here is
# queryable until `supabase db push` + the demo seed have been run).
# Client-side keys only — never the service-role key in this app.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
