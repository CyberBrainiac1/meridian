# ESP32-CAM Firmware

Firmware scaffolding for AI-Thinker-compatible ESP32-CAM boards such as the HiLetgo OV2640 module.

Copy `config.example.h` to `config.h` locally before flashing. `config.h` is ignored because it contains Wi-Fi credentials and device-specific settings.
