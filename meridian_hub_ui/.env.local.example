# Resident Hub uses its own Auth user, provisioned in resident_hub_devices.
# These are public client values only. Never put a service-role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
